diff --git a/src/geo/GModelIO_OCC.cpp b/src/geo/GModelIO_OCC.cpp
index a2cb1260c..1bd160319 100644
--- a/src/geo/GModelIO_OCC.cpp
+++ b/src/geo/GModelIO_OCC.cpp
@@ -4551,6 +4551,8 @@ bool OCC_Internals::remove(const std::vector<std::pair<int, int>> &dimTags,
   return ret;
 }
 
+#if 0 // STEP/IGES (DataExchange) disabled in this build — unused by the OCC-fragment
+      // mesher; removing it trims the STEP/IGES OCC libs (~several MB) from the WASM binary.
 static void setTargetUnit(const std::string &unit)
 {
   if(unit.empty()) return; // use unit specified in the file
@@ -4657,6 +4659,7 @@ static void setOCCSTEPHeader(STEPControl_Writer &writer)
   model->AddHeaderEntity(hdesc);
   model->AddHeaderEntity(hschema);
 }
+#endif // STEP/IGES helpers disabled
 
 #if defined(HAVE_OCC_CAF)
 
@@ -4870,6 +4873,7 @@ bool OCC_Internals::importShapes(const std::string &fileName,
       BRep_Builder aBuilder;
       BRepTools::Read(result, fileName.c_str(), aBuilder);
     }
+#if 0 // STEP/IGES read disabled (DataExchange trimmed from this build)
     else if(format == "step" || split[2] == ".step" || split[2] == ".stp" ||
             split[2] == ".STEP" || split[2] == ".STP") {
       STEPControl_Reader reader;
@@ -4918,6 +4922,7 @@ bool OCC_Internals::importShapes(const std::string &fileName,
       reader.TransferRoots();
       result = reader.OneShape();
     }
+#endif // STEP/IGES read disabled
     else {
       Msg::Error("Unknown file type '%s'", fileName.c_str());
       return false;
@@ -5187,6 +5192,7 @@ bool OCC_Internals::exportShapes(GModel *model, const std::string &fileName,
     else if(format == "xao" || split[2] == ".xao" || split[2] == ".XAO") {
       _writeXAO(c, model, fileName);
     }
+#if 0 // STEP/IGES write disabled (DataExchange trimmed from this build)
     else if(format == "step" || split[2] == ".step" || split[2] == ".stp" ||
             split[2] == ".STEP" || split[2] == ".STP") {
       STEPControl_Writer writer;
@@ -5224,6 +5230,7 @@ bool OCC_Internals::exportShapes(GModel *model, const std::string &fileName,
         return false;
       }
     }
+#endif // STEP/IGES write disabled
     else {
       Msg::Error("Unknown format to export OpenCASCADE shapes: %s",
                  format.c_str());
